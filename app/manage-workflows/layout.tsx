import { Layout } from "@/components/layout"
import type { ReactNode } from "react"

export default function ManageWorkflowsLayout({ children }: { children: ReactNode }) {
  return <Layout>{children}</Layout>
}

